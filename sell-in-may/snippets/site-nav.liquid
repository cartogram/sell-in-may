{% comment %}
List out your main-menu linklist (default)

More info on linklists:
- http://docs.shopify.com/themes/liquid-variables/linklists
{% endcomment %}
<ul class="nav nav--global">
    {% for link in linklists.main-menu.links %}
    {% comment %}
    Create a dropdown menu by naming a linklist the same as a link in the parent nav

    More info on dropdowns:
    - http://docs.shopify.com/manual/your-website/navigation/create-drop-down-menu
    {% endcomment %}
    {% assign child_list_handle = link.title | handleize %}
    {% if linklists[child_list_handle].links != blank %}
    <li class="nav__item nav__item--has-dropdown{% if link.active %} nav__item--is-active{% endif %}" aria-haspopup="true">
        <a href="{{ link.url }}">{{ link.title }}</a>
        <ul class="nav__dropdown">
            {% for childlink in linklists[child_list_handle].links %}
            <li {% if childlink.active %}class="nav__item--is-active"{% endif %}><a href="{{ childlink.url }}">{{ childlink.title | escape }}</a></li>
            {% endfor %}
        </ul>
    </li>
    {% else %}
    <li class="nav__item {% if link.active %} nav__item--is-active{% endif %}">
        <a href="{{ link.url }}">{{ link.title }}</a>
    </li>
    {% endif %}
    {% endfor %}
    <li class="nav__item nav__item--more ">
        <a class="show-for-small-only" cg-toggle state="state-nav-secondary-is-shown">MORE</a>
    </li>
</ul>
